spring:
  application:
    name: gateway-server
  cloud:
    config:
      uri: http://host.docker.internal:8888
      enabled: true
      name: gateway-server
    loadbalancer:
      ribbon:
        enabled: false
